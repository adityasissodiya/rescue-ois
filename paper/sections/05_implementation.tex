\section{Prototype and Evaluation}
\label{sec:implementation}

\subsection{Prototype and Emulation Setup}
\label{sec:eval-env}
\label{sec:validation}
\label{sec:evaluation}

The artifact repository contains a prototype service path for the core and edge
tiers, plus deployment scaffolding for the target vehicle environment. Python
services target Python~3.12 and use FastAPI and asyncpg. Database schemas are
managed through numbered SQL migrations. Docker
Compose files instantiate the emulated core, command edge, and responder edges
used in the emulation setup below; a separate \path{baseline/crdt} Compose stack
provides the CRDT-LWW comparison point.

Table~\ref{tab:impl-mapping} records the prototype evidence boundary for each
protocol concept from \S~\ref{sec:architecture}. We use three evidence levels
throughout: \emph{measured} (exercised on
the prototype service path), \emph{model-level} (established by the TLA+/Hypothesis
checks but not the service path), and \emph{deployment-only} (target-deployment
design, not exercised here). Later references to the evidence boundary reuse this
taxonomy.

\begin{table}[!htbp]
\centering
\caption{Prototype evidence boundary for each protocol concept.}
\label{tab:impl-mapping}
\scriptsize
\setlength{\tabcolsep}{2pt}
\begin{tabularx}{\columnwidth}{@{}p{0.34\columnwidth}X@{}}
\toprule
\textbf{Concept} & \textbf{Status} \\
\midrule
\multicolumn{2}{@{}l}{\textit{M1--M2: Linearization boundary and incident-journal sequencing}} \\
\midrule
Incident journal and \textit{event\_seq} &
Implemented; evaluated on the command-edge \textit{syncd} accept path. \\
Command-edge accept during responder partition &
Evaluated for the direct command-edge \textit{syncd} accept path during responder isolation; tablet-facing command-edge \textit{ops-api} not exercised. \\
\midrule
\multicolumn{2}{@{}l}{\textit{M3: Durable outbox and idempotency}} \\
\midrule
Responder durable outbox &
Implemented; evaluated for responder forwarding. \\
\textit{client\_event\_id} idempotency &
Implemented; evaluated by duplicate replay (\S~\ref{sec:eval-m3}). \\
\midrule
\multicolumn{2}{@{}l}{\textit{M4: Fenced promotion}} \\
\midrule
Promotion and fencing &
Durable command-epoch table and stale-epoch rejection implemented at service level; matching TLA+/Hypothesis model checks; measured in \S~\ref{sec:eval-promotion}. \\
\midrule
\multicolumn{2}{@{}l}{\textit{Supporting infrastructure (not authority-aligned)}} \\
\midrule
Core bootstrap and backhaul &
Synthetic bootstrap and incident-journal backfill; evaluated in Docker. \\
Vehicle edge roles &
Command and responder edges in Compose. \\
Incident-journal forwarding to core &
Implemented; evaluated for WAN recovery/backfill. \\
Tablet client &
Python stub in evaluation; Android scaffold only. \\
Multi-condition netem evaluation &
Implemented; measured under two link profiles in \S~\ref{sec:eval-m3}. \\
Transit and security controls &
Target-deployment documentation and configuration; physical mesh and security overhead are out of scope for the reported evaluation. \\
\bottomrule
\end{tabularx}
\end{table}

The evaluated path is narrower than the full target deployment: \textit{sync-api}
bootstrap and backfill, the responder \textit{ops-api} outbox with
\textit{client\_event\_id} idempotency, the \textit{syncd} forward path, the
command-edge \textit{event\_seq} assignment, and the command-to-core backfill.
Table~\ref{tab:impl-mapping} records each concept's evidence status.

Security controls (VPN overlay, mTLS termination, certificate-derived identity)
are deployment design, not measured here; the emulation uses plain HTTP on a
Docker bridge with synthetic device and user identifiers.
