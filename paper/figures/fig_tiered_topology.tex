% Tiered edge topology: core / vehicle edges / leaf tablets.
% The command edge owns the journal; the mesh is routed transit, not a
% consistency mechanism.
\begin{tikzpicture}[
  every node/.style={font=\scriptsize},
  box/.style={draw, rounded corners=1pt, align=center, inner sep=2.5pt, minimum height=6.5mm},
  core/.style={box, fill=gray!15, text width=36mm},
  cmd/.style={box, double, double distance=0.6pt, text width=26mm},
  resp/.style={box, text width=26mm},
  tab/.style={box, fill=gray!5, text width=18mm, minimum height=5mm},
  link/.style={<->, >=Stealth, semithick},
  up/.style={->, >=Stealth, semithick}
]
\node[core] (core) at (4.3,1.5) {Regional core\\master / reference data \textperiodcentered{} receives backhaul};
\node[cmd]  (cmd)  at (1.9,-0.4) {Command edge\\owns append-only journal};
\node[resp] (resp) at (6.7,-0.4) {Responder edge\\queues submissions durably};
\node[tab]  (t1) at (1.9,-2.2) {Tablet (leaf)};
\node[tab]  (t2) at (6.7,-2.2) {Tablet (leaf)};

\draw[up] (cmd)  -- node[left,  font=\tiny] {backhaul} (core);
\draw[up] (resp) -- node[right, font=\tiny] {backhaul (online)} (core);
\draw[link] (cmd) -- node[above, font=\tiny, align=center] {mesh = routed transit\\(no extended VLAN)} (resp);
\draw[up] (t1) -- node[left,  font=\tiny] {local services} (cmd);
\draw[up] (t2) -- node[right, font=\tiny] {local services} (resp);
\end{tikzpicture}
