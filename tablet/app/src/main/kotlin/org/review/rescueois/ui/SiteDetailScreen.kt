package org.review.rescueois.ui

import androidx.compose.runtime.Composable

/**
 * Detail view for selected site — plans, hazards, attachments.
 */
@Composable
fun SiteDetailScreen(siteId: String) {
    // TODO: load site from SiteDao, render plans / hazards / attachment list
}
