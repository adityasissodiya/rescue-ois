package org.review.rescueois.ui

import androidx.compose.runtime.Composable

/**
 * Persistent status bar showing sync state, last sync time, connectivity indicator.
 */
@Composable
fun SyncStatusBar() {
    // TODO: observe SyncManager state, render badge with color, last sync timestamp
}
