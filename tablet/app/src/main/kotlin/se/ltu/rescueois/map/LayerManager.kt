package se.ltu.rescueois.map

/**
 * Manages MapLibre style layers (sites, hazards, area geometries, incident overlay).
 */
class LayerManager {
    fun toggleLayer(layerId: String, visible: Boolean) {
        // TODO: flip MapLibre layer visibility
        throw NotImplementedError()
    }
}
