package se.ltu.rescueois.map

/**
 * Wraps MapLibre Native Android initialization with an offline tile source
 * sourced from the local PMTiles cache.
 */
class MapLibreWrapper {
    fun initialize() {
        // TODO: configure MapLibre with OfflineTileProvider as the only source
        throw NotImplementedError()
    }
}
