package se.ltu.rescueois.ui

import androidx.compose.runtime.Composable

/**
 * Active incident view — event log, status updates, field edit submission.
 */
@Composable
fun IncidentScreen(incidentId: String) {
    // TODO: stream events from IncidentDao, render event log, support field edit submission
}
